using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P1_Introduccion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int s;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
